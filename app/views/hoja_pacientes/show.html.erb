<p style="color: green"><%= notice %></p>

<%= render @hoja_paciente %>

<<div class="container">
<div class="mb-3">
<%= link_to "Agregar Exploración Física", new_exploracion_fisica_path(hoja_paciente_id: @hoja_paciente.id), class: 'btn btn-secondary' %>
</div>
<div class="mb-3">
<%= link_to "Crear Video", new_video_path(hoja_paciente_id: @hoja_paciente.id), class: 'btn btn-secondary' %>
<br>
</<div class="container">
  <%= link_to "Edit this hoja paciente", edit_hoja_paciente_path(@hoja_paciente) %> |
  <%= link_to "Regresar a lista de pacientes", pacientes_path  %>
  <%= button_to " Borrar hoja paciente", @hoja_paciente, method: :delete %>
</div>
